class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        res = []

        for i in range(len(nums)):
            if nums[i] != val:
                res.append(nums[i])

        k = len(res)

        for i in range(k):
            nums[i] = res[i]

        return k
